{
  "name": "single-screen-games",
  "version": "1.0.0",
  "description": "Single screen games with Phaser 3 & TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "phaser": "^3.55.2",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  }
}
